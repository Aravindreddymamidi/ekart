import { AddProductRequest } from './add-product-request';

describe('AddProductRequest', () => {
  it('should create an instance', () => {
    expect(new AddProductRequest()).toBeTruthy();
  });
});
